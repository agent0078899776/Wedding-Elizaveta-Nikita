{
  "entities": {
    "rsvp": {
      "title": "RSVP Submission",
      "description": "Guest attendance confirmation and event-day preferences",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Full name of the guest"
        },
        "attending": {
          "type": "boolean",
          "description": "Whether the guest is attending"
        },
        "guestsCount": {
          "type": "integer",
          "description": "Number of additional guests accompanying them (plus-one)"
        },
        "drinks": {
          "type": "array",
          "description": "Preferred beverages selected by the guest"
        },
        "dietary": {
          "type": "string",
          "description": "Dietary restrictions or food preferences"
        },
        "transport": {
          "type": "boolean",
          "description": "Whether the guest needs shuttle service"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the RSVP was submitted"
        }
      },
      "required": ["name", "attending", "createdAt"]
    },
    "wish": {
      "title": "Guestbook Wish",
      "description": "A congratulatory wish left on the guestbook for Nikita and Elizaveta",
      "type": "object",
      "properties": {
        "author": {
          "type": "string",
          "description": "Name of the person leaving the wish"
        },
        "message": {
          "type": "string",
          "description": "Message or advice for the couple"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the wish was written"
        }
      },
      "required": ["author", "message", "createdAt"]
    }
  },
  "firestore": {
    "/rsvps/{rsvpId}": {
      "schema": "rsvp",
      "description": "Collection of guest RSVP submissions"
    },
    "/wishes/{wishId}": {
      "schema": "wish",
      "description": "Collection of guest wishes in the guestbook"
    }
  }
}
